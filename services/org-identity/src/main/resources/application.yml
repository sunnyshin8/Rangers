server:
  port: 8081
spring:
  application:
    name: org-identity
  datasource:
    url: jdbc:postgresql://localhost:5432/leakradar
    username: leakradar
    password: leakradar
leakradar:
  jwt:
    secret: ${JWT_SECRET:leak-radar-dev-secret-change-in-production-min-32-chars}
    expiration-ms: 86400000
